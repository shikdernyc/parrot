import React, { Component, Fragment } from 'react';

class Dashboard extends Component {
  render () {
    return (
      <Fragment>
        <h2>Agent Landing Page</h2>
        <p>Select a domain to get started</p>
      </Fragment>
    );
  }
}

export default Dashboard;
